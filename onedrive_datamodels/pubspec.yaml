name: onedrive_datamodels
description: A starting point for Dart libraries or applications.
version: 0.0.1
# homepage: https://www.example.com

environment:
  sdk: ">=2.8.1 <3.0.0"

dependencies:
  json_annotation: ^3.0.1

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
  json_serializable: ^3.4.1
  build_runner: ^1.10.2
