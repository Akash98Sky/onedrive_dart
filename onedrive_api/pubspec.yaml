name: onedrive_api
description: A starting point for Dart libraries or applications.
version: 0.0.1
# homepage: https://www.example.com
environment:
  sdk: ">=2.8.1 <3.0.0"

dependencies:
  dio: ^3.0.10
  retrofit: ^1.3.4
  onedrive_datamodels:
    path: ../onedrive_datamodels

dev_dependencies:
  pedantic: ^1.8.0
  test: ^1.6.0
  build_runner: ^1.10.2
  retrofit_generator:
    path: ../../retrofit.dart/generator/
    # git:
    #   url: https://github.com/Akash98Sky/retrofit.dart
    #   path: generator
